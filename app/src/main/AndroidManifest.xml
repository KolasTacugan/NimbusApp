<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nimbus">
        <activity
            android:name=".ForgotPassword.ForgotPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".History.HistoryActivity"
            android:exported="false" />
        <activity
            android:name=".EditProfile.EditProfileActivity"
            android:exported="false" />
        <activity
            android:name=".Settings.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".Profile.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".Dashboard.DashboardActivity"
            android:exported="false" /> <!-- Login screen should open first -->
        <activity
            android:name=".Login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Nimbus">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MainActivity SHOULD NOT launch first -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Nimbus" />
    </application>

</manifest>